[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# dealers.whitegloveauto.app maps to this site in the Netlify dashboard
# (Domain management → add custom domain). The plugin handles routing
# for the App Router, API routes, and server components automatically.
